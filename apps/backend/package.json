{
  "name": "@fright-rate/backend",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "serve": {
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "@fright-rate/backend:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "@fright-rate/backend:build:development"
          },
          "production": {
            "buildTarget": "@fright-rate/backend:build:production"
          }
        }
      }
    }
  }
}
